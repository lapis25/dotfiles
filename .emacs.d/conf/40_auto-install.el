;; auto-install
(require 'auto-install)
(setq auto-install-directory "~/.emacs.d/install-lisp/")
(auto-install-update-emacswiki-package-name t)

;;; install memo
;; apel http://kanji.zinbun.kyoto-u.ac.jp/~tomo/elisp/APEL/
;; make
;; make install LISPDIR=~/.emacs.d/install-lisp VERSION_SPECIFIC_LISPDIR=~/.emacs.d/install-lisp
